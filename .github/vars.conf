# This file is automatically generated (based on this file)

# ~~~~~~~~~~~~~~~~~~~~~~~
# auto-builder.yml
# ~~~~~~~~~~~~~~~~~~~~~~~

BUILD_SCHEDULE='0 0 * * 0'
BRANCH='groovy'
DOCKERHUB_IMAGE='baseimage-ubuntu'
DOCKERIMAGE_TAG='latest'
APP_VERSION_LINK='$(curl -sX GET "https://api.github.com/repos/just-containers/s6-overlay/releases/latest" | jq -r .tag_name)'
PLATFORMS='linux/amd64,linux/arm64,linux/arm/v7'
APP_BRANCH=''
BASE_OS='ubuntu'
ON_RELEASE='false'
VERSIONING='false'
BUILD_ARGS='--build-arg UBUNTU_VERSION=groovy
--build-arg OVERLAY_VERSION=$(curl -sX GET "https://api.github.com/repos/just-containers/s6-overlay/releases/latest" | jq -r .tag_name)
--tag vcxpz/baseimage-ubuntu:groovy'

# ~~~~~~~~~~~~~~~~~~~~~~~
# auto-release.yml
# ~~~~~~~~~~~~~~~~~~~~~~~

RELEASE_SCHEDULE='0 0 * * 1-6'
RELEASE_VERSION=''

# ~~~~~~~~~~~~~~~~~~~~~~~
# README.md
# ~~~~~~~~~~~~~~~~~~~~~~~


